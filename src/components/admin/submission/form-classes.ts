/** Shared control styling for the reconciliation screens, so every input reads the same. */
export const inputClass =
  "border-input bg-card focus:border-ring h-11 w-full rounded-md border px-3 text-base outline-none transition-colors";

export const labelClass =
  "text-muted-foreground text-xs font-semibold tracking-[0.1em] uppercase";
