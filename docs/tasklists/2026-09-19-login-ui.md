# Tasklist — login UI: buyer phone-OTP, developer and admin email/password

**Status:** core complete 2026-09-19 — header session state deferred (see below)
**Owner:** Bhavarth (auth touches review-required territory per `AGENTS.md`)
**Branch:** task/phase-2a-completion
**Parent:** `docs/tasklists/2026-09-18-phase-2a-completion.md`, step 1
**References:** `src/lib/auth.ts`, `docs/app-flows/buyer.md` (OTP failure: "preserve return destination and allow retry without recording an unlock"), `docs/app-flows/developer.md`, `docs/app-flows/admin.md`, `docs/design/design-tokens.md`, `_stitch_export/stitch_propcompare_residence_concierge/guided_residence_concierge_2/DESIGN.md`, `docs/tasklists/2026-09-18-pre-login-intake-cookie.md`, `DECISIONS.md` 2026-09-19

## Decisions already made (2026-09-19, user sign-off)

- Three separate routes: `/login` (buyer, phone OTP), `/developers/login` and `/admin/login` (email + password). The word "staff" is not used in the UI or in route names.
- A user's role is the presence of a row in `developer_users` or `admin_users`. A developer account signing in at `/admin/login` (or the reverse) is rejected with the same generic error as a bad password — the screen does not reveal which role an email has.
- Developers are invited by an admin (separate step in the parent tasklist); there is no public developer or admin sign-up. Public email/password sign-up must therefore be disabled in `src/lib/auth.ts`, not merely unlinked.
- Design: follow the Soft Daylight tokens already in `globals.css`. The three screens share one layout so they read as one product, not three bolted-on forms.

## Open items (surface, do not guess)

- **First-admin bootstrap (done: `bun run db:first-admin`, approved 2026-09-19).** With sign-up disabled, the first admin needs a path. Proposed: a one-off maintainer script that creates the `users` + `admin_users` rows (identity only — it writes no live catalog table, so the one-write-path rule is not touched). Needs sign-off.
- **Invite set-password link** belongs to the developer-onboarding step, not this one; this tasklist only builds the sign-in screens and the `/developers/set-password` landing they will hand off to is deferred.
- **SMS provider** is still unchosen (`sendOTP` logs in dev, throws in production). The OTP screen is built against Better Auth's `phoneNumber` plugin; production OTP delivery stays blocked on that decision.

## Checklist

- [x] Disable public email/password sign-up in `src/lib/auth.ts`; test that `POST /api/auth/sign-up/email` is refused.
- [x] Server helper resolving a session to a role (`buyer` / `developer` + linked `developerId` / `admin` + permission level), reading `developer_users` and `admin_users`; unit tests for each role and for a user with none.
- [x] Route guards: `/developers/*` and `/admin/*` (except their login pages) redirect to the matching login; wrong-role sessions are refused.
- [x] Shared auth layout component (chalk canvas, white panel, Cormorant heading, Plus Jakarta body, terracotta primary button, `label-caps` labels, 8px radius).
- [x] `/login`: phone entry → OTP entry → session. Preserves the return destination; a failed or expired OTP allows retry without recording an unlock. Claims the pre-login intake cookie only if that tasklist has landed.
- [x] `/developers/login` and `/admin/login`: email + password, generic error, role check on success.
- [x] Done 2026-09-19: session-aware header state (signed-in indicator, sign-out). Must be a client-side session read — a server read in `SiteHeader` would make every buyer page dynamic and defeat the dossier's ISR. Touches Deep's header; coordinate. Coordinate with Deep for buyer entry points; do not duplicate.
- [x] UI tests: each screen's success, wrong-credentials, wrong-role, OTP failure and retry paths; a11y basics (labelled inputs, error announced).
- [x] Integration test with a real Better Auth session for the role helper.
- [x] Update `PROGRESS.md`; mark this tasklist complete with verification notes.

## Added 2026-09-19 after review

- [x] Buyer sign-out, and a signed-in indicator (first name only; never the phone number).
- [x] First sign-up asks for a name (required) and email (optional); `changeEmail` enabled for unverified accounts only.
- [ ] Buyer-facing saved properties / comparisons screens so a returning buyer sees their activity — backend done, UI is Deep's Phase 3 scope (retention feature, do not let it slip).
- [ ] Email verification for buyer emails — needs an email provider (see `docs/production-readiness.md`).
