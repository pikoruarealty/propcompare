# Decisions

Lightweight ADR log. Every decision that would be expensive to silently reverse gets an entry here, in order, never edited after the fact — a later decision that changes an earlier one adds a new entry and marks the old one superseded. This exists because the prior attempt at this product (`pikorua-luxe-compare`) pivoted infrastructure (Supabase → self-hosted Postgres) with no decision record, discovered only via a panicked migration 4 days before development stalled. See `docs/postmortem-2026-08.md` if/when that debrief is written up in full.

Format: **Date — Decision.** Context. Why. Alternatives considered.

---

**2026-08-31 — Rebuild from scratch; old project (`pikorua-luxe-compare`) is requirements/lessons only, not a template.**
Context: prior attempt stalled after ~5.5 weeks. Why: its own final handoff note recommended exactly this; root failure was two live schemas of the same entity coexisting and diverging, not a fixable code issue. Alternatives: fork and refactor — rejected, the dual-schema wound was structural.

**2026-08-31 — Full three-role platform (buyer / developer / admin), sequenced buyer-first.**
Buyer experience + admin-run OCR ingestion ship first; developer self-serve submission portal ships later. Why: brochures + GujRERA are the only data sources available, so an ingestion pipeline is required from day one regardless — but a developer-facing UI for it is not required to launch to buyers.

**2026-08-31 — Self-hosted PostgreSQL, own VM/Docker (not managed/hosted DB).**
Matches the old project's own concluded-correct infra choice, made independent of this decision.

**2026-08-31 — Next.js (App Router) over TanStack Start for the frontend.**
Why: SEO/ISR maturity matters for organic search on property pages; Next.js has far greater representation in AI-agent training data, reducing hallucination risk with two AI coding agents (Claude Code + Codex) writing this codebase in parallel. Drizzle ORM, Better Auth, and Google Cloud Storage for media all carried forward unchanged from the old stack.

**2026-08-31 — OCR ingestion targets a predefined, versioned field schema; anything outside it is discarded, not stockpiled.**
Reacting directly to the old project's outcome: 824 fields flagged for review against only 315 auto-accepted. Enforced in schema via `property_schema_fields` (see `docs/schema/schema.v1.md`) — the OCR pipeline extracts only what's listed there.

**2026-08-31 — GujRERA data is fetched via a structured scrape/fetch job and used only as an independent cross-check against brochure OCR — never blanket "verified" badging.**
A `rera_fetch_jobs` row that disagrees with a brochure becomes a new `property_submissions` row (source = `rera_scrape`), subject to the same admin approval as any other change — RERA fetches never write live data directly.

**2026-08-31 — Geography fixed to Ahmedabad/Gujarat only; target buyers broadened from ultra-luxury-only to the full regular-to-ultra-luxury spectrum; property types fixed to Apartment / Bungalow / Plot (lookup table, softly extensible).**
Location is still modeled as structured fields (city, locality, lat/long, pincode) rather than free text, to support map/locality search later without a schema change now.

**2026-08-31 — Exact unit prices are never shown in the app. Price data exists only to bucket properties against a buyer's stated budget during discovery/comparison matching.**
On the property detail page specifically, the default is **zero price shown at all**, until the buyer submits an enquiry — stricter than "show a bucket." Commercial data (`unit_price_history`) lives in a separate `private` Postgres schema, RLS enabled with zero policies (deny-by-default, service-role only), reusing the old project's validated isolation pattern. Buyer-facing bucket matching reads only a derived `private.unit_current_bucket` view via a single service-role code path — no other code path may query `private`. Default may change later per explicit product decision, not silently.

**2026-08-31 — Developer portal and admin/verification portal are two separate, distinctly-permissioned interfaces, not one shell gated by role.**
Context: the Stitch UI export's "Editorial Desk" shell conflated developer-portfolio-analytics and admin-OCR-verification into one sidebar; on inspection this was generation noise (Stitch reusing a convenient shell across unrelated concepts), not an intentional merged design. Decided to build them as genuinely separate surfaces with separate permission models, matching the draft→submit→approve→publish trust boundary where a developer must never have review/approve capability over their own submissions.

**2026-08-31 — Design system: "Soft Daylight" v2 (Cormorant Garamond + Plus Jakarta Sans, Soft Gold reserved for Verified badges) is canonical.**
Two competing token specs existed in the Stitch export; v2 was chosen because the actual reviewed screens (gold-toned RERA Verified badge) matched it, and it includes the more developed component set (PropScoreDial, evidence-viewer treatment). Full tokens: `docs/design/design-tokens.md`.

**2026-08-31 — One canonical write path into live catalog tables: the `property_submissions` publish transaction. No migration/seed/backfill script may write to `properties` or its child tables directly.**
This is the direct fix for the old project's root failure: a backfill script called the "V2 publish" path directly, bypassing developer review and admin approval entirely. Schema-enforced via `property_revisions` (a snapshot is written every time, in the same transaction, as an audit trail); process-enforced via `AGENTS.md` for any human or AI contributor.

**2026-08-31 — Original whiteboard schema image included a `Reviews` entity and an unlabeled RERA-extract fields block; neither made it into `schema.v1.md`'s first pass, and their resolution wasn't written down anywhere. Re-derived from a re-shared photo of the whiteboard and closed out here.**
Context: this is a direct instance of the failure mode `AGENTS.md` line 20 exists to prevent — the earlier resolution of these fields happened in conversation and was never given a dated entry, so it was lost to context compaction and had to be re-derived from the original whiteboard photo. Resolutions, applied to `docs/schema/schema.v1.md`:

- `reviews` table added (property-level, not unit-variant-level, matching the whiteboard exactly: `id, property_id, user_id, rating, remarks`) — extended with verification fields (`verification_status`, `verification_document_id` reusing `source_documents`, `verified_by`, `verified_at`) rather than shipped as a bare star-rating table, per the explicit decision to build in verification from day one rather than defer it.
- The whiteboard's unlabeled blue-ink block (`rera_id?, progress?, project_land_area, carpet_area_range, amenities{}, builder_details{}, total_units, total_towers, project_status`) is RERA-extract data at the project level. `total_units`/`total_towers`/`possession_date` were already columns on `properties`; `amenities{}` and `builder_details{}` are already covered by `property_amenities` and `developers` respectively (not duplicated); `project_status` maps to the existing `possession_status` enum. The two genuinely new facts — `project_land_area` and `carpet_area_range` — plus `progress` (construction completion %) are added as new nullable `properties` columns (`rera_project_land_area_sqft`, `rera_carpet_area_range_min_sqft`/`_max_sqft`, `rera_construction_progress_percent`), populated only when `rera_registered` is true, consistent with whiteboard Q1's "RERA fields live directly on `properties`" resolution.
- The whiteboard's `Configurations` entity named layout forms (Penthouse, Duplex) alongside N-BHK counts. `bhk_types` stays bedroom-count-only; a new `layout_types` lookup was added instead of overloading `bhk_types`, and `unit_variants` now carries both `bhk_type_id` and `layout_type_id` independently (a unit can be a 3BHK duplex, for example).

Why this entry is longer than usual: it exists specifically to make sure this exact loss doesn't happen again — anyone re-deriving these fields from a whiteboard photo in the future should find them here first.

**2026-09-01 — Private-schema access uses separate PostgreSQL app and service roles.**
Context: PostgreSQL table owners bypass row-level security by default. The initial local `DATABASE_URL` used the Docker bootstrap/owner role, so RLS with zero policies would not actually prevent regular application code from reading exact prices. Resolution: the normal application connection is `propcompare_app`, which receives public-schema privileges and no `private` schema usage; migrations use the non-runtime `propcompare` admin role; the future discovery/comparison service alone uses `propcompare_service`, a narrowly reserved `BYPASSRLS` role. `private` tables have RLS enabled and forced with zero policies. Why: preserves the already-decided rule that exact prices never reach ordinary app code while using PostgreSQL's own enforcement rather than a convention. Alternatives: retain one owner connection and rely on code discipline — rejected because it defeats the stated RLS boundary.

---

**2026-09-01 — Phase 1 does not use a direct catalog fixture to test the private bucket mapping.**
Context: `private.unit_price_history` correctly requires an existing published `unit_variants` row. Creating that row with a seed script, migration, or direct SQL insert would violate the one-write-path rule before the Phase 2 publish transaction exists. Resolution: Phase 1 verifies the view definition and effective role/RLS boundary; an end-to-end current-price-to-bucket mapping test is deferred until Phase 2 can create a fixture through `property_submissions` publication. Why: a test convenience cannot become an exception to the project’s primary trust boundary. Alternatives: direct temporary catalog insertion or a special test backdoor — rejected.

---

**2026-09-01 — V1 uses a concise, buyer-facing amenity catalog; uncommon or ambiguous brochure items remain review-only.**
Context: the legacy OCR audit found 789 distinct amenity labels, including facilities mixed with room details, measurements, marketing language, and nearby-place claims. Resolution: seed a deliberately limited set of roughly 20–30 buyer-useful facility concepts and their approved synonyms; do not create a new filter simply because it occurs in a brochure. Unmatched text remains source-review material and may later justify a catalog addition through an explicit product decision. Why: useful discovery filters need stable, comparable meanings. Alternative: reproduce every brochure label as an amenity — rejected because it would turn the catalog into an unfilterable, inconsistent free-text mirror.

---

**2026-09-01 — Access and safety facilities are buyer-facing amenity filters in V1.**
Resolution: include security, visitor parking, and service lift in the concise amenity catalog, subject to the same normalized-key and synonym rules as other amenities. Why: they are buyer-relevant and comparable across properties. They are not treated as decorative marketing claims or inferred when absent.

---

**2026-09-01 — V1 amenity catalog size is not capped; canonical normalization, not a numerical limit, prevents duplicates.**
Context: the earlier concise-catalog decision used a rough 20–30-item starting scope. The product direction is now to retain as many meaningful amenities as are useful, provided their names remain consistent. Resolution: the approved 26-item list is an initial seed, not a ceiling. Each meaningful facility receives one lowercase `snake_case` key and one buyer-facing label; casing, spacing, punctuation, and approved wording variants map through `amenity_synonyms`. A new semantic concept may be added only through a documented catalog review, never automatically from an unmatched OCR string. Non-amenity room details, counts, measurements, brands, nearby places, and marketing claims remain excluded. This supersedes only the numerical cap in the 2026-09-01 concise-catalog decision.

---

**2026-09-01 — Buyer budget matching uses an inclusive ±20% expansion of the buyer's stated range.**
Resolution: for buyer range `[min, max]`, the Phase 3 service matches current unit prices in `[min × 0.80, max × 1.20]`, inclusive. Thus ₹3–4 crore matches ₹2.4–4.8 crore. The calculation and exact prices remain entirely in the private service-only path; it returns only property/unit identifiers, never a price or derived price range. The existing `private.unit_current_bucket` view remains a coarse classification aid, but the earlier adjacent-bucket candidate-selection approach is superseded because it cannot guarantee this tolerance. The precise service-only matcher receives its own Phase 3 tasklist and review.

---

**Still open, not yet decided:** admin MFA enforcement timing (schema has the flag, default off); exact publish-transaction implementation; developer self-serve submission UI validation rules; whether `unit_variants.variant_name` needs a stronger identity guarantee across resubmissions (flagged as a known risk in `docs/schema/schema.v1.md`, deferred rather than solved).

---

**2026-09-01 — Budget-bucket bounds move from `public` to `private`; v1 uses fixed internal bands.**
Context: `budget_buckets` was initially a public lookup, and the normal application role can read all public tables. That contradicts the approved rule that price bounds must never appear in public schema or buyer-facing output. Resolution: schema v2 moves the sole `budget_buckets` table to `private`; the security-invoker `private.unit_current_bucket` joins it internally, and only the dedicated service role may read the resulting classification. The controlled admin seed path owns the initial fixed magnitude bands. Ranges are lower-inclusive and upper-exclusive to prevent overlapping classifications at a boundary. The Phase 3 ±20% private matcher is unchanged and remains the authoritative buyer-range matcher.

---

**2026-09-01 — Brochure OCR is routed by a human-confirmed, versioned page manifest before paid extraction.**
Context: brochure page boundaries are not unit-variant boundaries. A penthouse or duplex may span lower-floor, upper-floor, and terrace pages, while a brochure may also contain many pages irrelevant to the approved extraction contract. Resolution: cheap PDF text-layer inspection may suggest page scopes, but an uploader must confirm the routing manifest before extraction. A confirmed unit-variant scope contains one proposed variant identity and one or more ordered pages; the OCR adapter must return at most one variant candidate for that scope. Multiple pages therefore remain evidence for one proposed canonical `unit_variants` item rather than becoming separate variants. The routing manifest is stored with a pipeline and field-contract version on the OCR attempt and becomes immutable once queued. It is processing metadata only and never writes to the live catalog. Why: this reduces paid page processing and prevents document layout from silently defining catalog identity. Alternatives: OCR the entire brochure and infer grouping afterward, or treat every floor-plan page as a separate variant — rejected as costly and structurally unreliable.

---

**2026-09-01 — Historical OCR JSON is evaluation evidence only; every selected brochure is reprocessed through the new pipeline.**
Context: the existing property JSON files were produced by the retired pipeline and may contain exactly the grouping and normalization errors the new flow is intended to prevent. Resolution: historical output may be normalized into a derived comparison report against a new run, but it cannot create `property_submissions`, `property_submission_fields`, or live catalog data. The selected curator-owned brochure set will be processed again with one versioned pipeline/field contract so all reviewable submissions share the same structure. Disagreements are evaluation findings for admin review, not precedence rules in favor of either output. Why: importing historical JSON would preserve old structural errors and create two ingestion authorities. Alternative: reuse apparently complete historical results and rerun only failures — rejected because completeness does not establish structural consistency.

---

**2026-09-01 — Submission-field provenance is one-to-many evidence; OCR attempt status belongs to attempt rows, not source documents.**
Context: `property_submission_fields.source_page` can cite only one page, but one field—especially the `unit_variants` array—may depend on several brochure pages. `source_documents.ocr_status` also cannot truthfully represent retries or pipeline-version comparisons. Resolution: schema v3 replaces the scalar source-document/page/snippet columns with `property_submission_field_evidence`, whose rows cite a document page and optional JSON value path. It adds versioned `ocr_extraction_jobs`, each holding its confirmed routing manifest and attempt status, and removes aggregate OCR status from `source_documents`. Why: provenance and retry history become reproducible without duplicating a property or variant entity. Alternative: place page arrays inside field values or keep both document and attempt statuses — rejected because those approaches mix provenance into canonical values or create two live statuses for one process.

---

**2026-09-01 — Builder profiles exist independently from builder staff accounts; admin-run ingestion precedes self-serve onboarding.**
Context: the initial catalog will be curated by admins from builder brochures, including builders that have no account on PropCompare. Later, builders should upload, map pages, review their own OCR draft, and submit it for independent admin approval. Resolution: `developers` is the sole canonical builder/company profile and can be created or selected by an admin without a user account. A future `developer_users` invitation links an authenticated staff user to that existing profile; it never creates a second builder record. New-property submissions store the selected canonical developer profile separately from the raw, evidence-backed `developer.name` OCR field. Initially an owner admin may create, review, approve, and publish an admin-run submission, with all actions audited. Once builder staff are onboarded, they may create and submit drafts only; an admin retains approval and publish authority. Why: the initial data-collection workflow works without fake accounts, while the future portal has a clear ownership boundary. Alternatives: require every builder to sign up before curation, or use OCR spelling as a developer identity — rejected because both block reliable initial ingestion.

---

**2026-09-01 — Published-property updates are additive patches; omission never deletes brochure facts.**
Context: a later brochure often contains only a subset of a property’s facts, and OCR absence means “not found in this selected evidence,” not “not offered” or “no longer exists.” Resolution: on an existing property, a submission updates only the canonical fields it explicitly carries. Present amenities and specifications add or update evidence-backed facts; omitted ones stay unchanged. Present unit variants upsert only by exact reviewed `variant_name`; a new name adds a variant. Automatic variant deletion, renaming, or fuzzy matching is forbidden in v1 and requires an explicit future review action. A new property receives `not_stated` rows for unmentioned controlled catalog items, while explicitly extracted items become `available`; automatic negative claims are never inferred. Why: preserves catalog integrity while allowing new brochures to improve it incrementally. Alternatives: treat every brochure as a complete replacement or infer removals from missing OCR — rejected because either can silently erase correct published data.

---

**2026-09-01 — Submission review permissions and slug generation are fixed for Phase 2A.**
Resolution: a submitter moves a draft or changes-requested submission to `submitted`; an admin verifier or owner may move it into review and issue changes, rejection, or approval; only an owner may publish an approved submission. The initial owner-admin workflow may have the same actor in each step, but every transition remains timestamped and attributable. New properties receive a normalized name-derived slug; on a uniqueness conflict, the publisher appends a deterministic short suffix derived from the submission id and retries inside the transaction. Why: early operation remains practical without weakening the future separation between builder submitters and admin publishers.

---

**2026-09-02 — Phase 2B builds the real Drizzle read layer now; fixtures are typed test doubles, not a parallel data path.**
Context: the roadmap scheduled Phase 2B against fixtures so buyer UI would not wait on Phase 2A's writers. Phase 2A's publish transaction has since landed, so published catalog data can now be produced legitimately. Resolution: the buyer read functions are implemented as real read-only Drizzle queries over the published catalog, and fixtures satisfy the same exported TypeScript types rather than defining their own shape. Fixtures back component and route tests, so screen work still needs no running database. Why: this avoids building a read path that is thrown away in Phase 3, and prevents a fixture shape from silently diverging from the real one — the same divergence class that ended the prior attempt. Alternatives: fixtures only for the whole phase — rejected because it defers the real read layer and creates two shapes of one entity; database-only — rejected because it blocks all UI work on local Docker availability.

---

**2026-09-02 — shadcn/ui with Radix primitives is the buyer component foundation, restyled to Soft Daylight tokens.**
Context: `ARCHITECTURE.md` named shadcn/Radix from the outset, but nothing was ever installed, so the choice was still genuinely open at first use. Resolution: adopt shadcn/ui now and point its theme at the existing Soft Daylight CSS variables in `src/app/globals.css` rather than generating a second token set. `--color-verified-gold` is kept out of the generated component palette and remains reserved strictly for verified/trust badges. Why: Radix supplies accessible interactive primitives — dialog, popover, and the Phase 3 OTP gate — that are expensive and error-prone to rebuild, and confirming the already-documented intent avoids a silent divergence from `ARCHITECTURE.md`. Alternatives: hand-rolled Tailwind components only — rejected because it re-implements accessibility behavior this project already knows it needs.

---

**2026-09-02 — Buyer UI is tested in a jsdom environment with Testing Library, alongside the existing node-environment tests.**
Context: Vitest ran one node environment, correct for the data-layer and publish-transaction tests but unable to assert rendered output. Resolution: add jsdom and Testing Library as a separate Vitest project so the existing node tests continue to run unchanged. The price-restraint rule and the honest-incompleteness states (`not_stated`, `explicitly_not_offered`) are asserted against rendered output, not assumed. Why: "never render a price" and "never render a plausible placeholder" are product guardrails, and an unenforced guardrail is exactly the failure mode this project has already paid for once. Alternatives: node-environment logic tests only — rejected because these particular guardrails exist only in rendered output.

---

**2026-09-02 — v1 buyer listing filter set is fixed to city, locality, propertyType, bhk, possessionStatus, and repeatable amenity; type/BHK filters take the lookup `key`, not the UUID.**
Context: `docs/api/api-spec.v1.md`'s contract-change process blocks any consumer starting work on `GET /api/v1/properties` until its filters are fixed, and `prd.v1.md` only said browsing should be supported "as the read contract matures" — Phase 2B Step 1 needed to actually fix that list rather than leave it open-ended. Resolution: the six filters above are the complete v1 set (documented in full, with query-param types and match semantics, in `api-spec.v1.md`); repeated `amenity` narrows with AND semantics and only matches `status = "available"` rows. `propertyType` and `bhk` are matched against `property_types.key` / `bhk_types.key` rather than their UUID primary keys. Why: this set matches `prd.v1.md`'s stated browsing dimensions (Gujarat location, property type, configuration, possession, amenities) exactly, every field already exists in `catalog.ts`, and `key`-based filters keep listing URLs human-readable and stable across a future data re-seed, where UUIDs would not be. Alternatives: dropping `amenity` for v1 to simplify the first read layer — rejected, the user chose to keep the full set; filtering by `id` — rejected, opaque URLs with no offsetting benefit since the lookup tables are small and already keyed by `key` elsewhere.

---

**2026-09-02 — Drizzle's migration journal and latest snapshot are tracked in git; the five existing migrations are kept rather than squashed.**
Context: `.gitignore` had excluded `drizzle/meta/` since the first Phase 0 baseline commit, so the journal and snapshots were never committed. The consequences were latent but severe, and were reproduced rather than assumed: `bun run db:migrate` fails on any fresh checkout because drizzle-kit cannot determine what to apply, and `bun run db:generate` — having no snapshot to diff against — emitted `0000_overconfident_scrambler.sql`, a second migration numbered 0000 colliding with the existing `0000_watery_jack_flag.sql`, with a journal listing only the new file. Because migration `0001` is hand-written SQL (the `propcompare_service` read grant) that drizzle-kit cannot reproduce from the schema, a regenerated baseline silently drops it. Resolution: un-ignore `drizzle/meta/`; reconstruct `_journal.json` with all five real migrations at their actual commit timestamps, plus `0004_snapshot.json` capturing the current schema. Verified both directions — `db:generate` now reports no schema changes, and `db:migrate` against an empty throwaway database applied all five and produced a `pg_dump` structure identical to the working database. Snapshots for `0000`–`0003` are not reconstructible and are deliberately absent; `drizzle-kit generate` reads only the newest, so this is sufficient. Why: two migrations defining the same tables is the schema-divergence failure this project exists to prevent, and it was one `db:generate` away from entering the repository. Alternatives: squashing the five migrations into a single regenerated baseline — rejected because it discards migration `0001`'s hand-written grant unless re-added by hand, and forces every developer to reset their local database for no compensating benefit while the project is still pre-deployment.

---

**2026-09-02 — Local database provisioning creates schemas, roles, and ownership only; all table privileges come from migrations.**
Context: standing up a native (non-Docker) local Postgres this session, the bootstrap added `ALTER DEFAULT PRIVILEGES` grants as a convenience. Comparing that database against one built purely by `bun run db:migrate` showed the convenience had granted `propcompare_service` INSERT/UPDATE/DELETE on all 36 public tables, where the migrations grant it `SELECT` on `public.unit_variants` alone. Resolution: provisioning scripts stop at schemas, roles, ownership, and `CONNECT`; migrations `0002`/`0003` own every table-level grant, and the local database was rebuilt through `db:migrate` to match. Why: the buyer/service/app privilege split is a security boundary described in `ARCHITECTURE.md`, and a local environment that quietly widens it cannot catch a permission regression that production would — the local setup must fail the same way production fails. Alternatives: keeping the blanket grants for convenience — rejected, it makes the documented privilege-split verification meaningless.

---

**2026-09-02 — Buyer read routes are request-time handlers whose caching is expressed as HTTP `Cache-Control` for a shared cache; Next.js-level ISR is not used on the JSON routes.**
Context: SEO/ISR maturity was an explicit reason for choosing Next.js (2026-08-31), and Phase 2B step 3 could not ship two public read routes without deciding what caches them. Three things constrained the choice, each checked against the bundled Next 16 docs rather than assumed: `cacheComponents` is not enabled, so `GET` Route Handlers already run at request time by default; `dynamic = "force-static"` is unavailable to the listing route in principle, because a force-static handler cannot read `request.nextUrl.searchParams` and that route is defined entirely by its query parameters; and prerendering the dossier route with `generateStaticParams` would require Postgres reachable at build time. Resolution: neither route exports a route segment config. Successful listing responses carry `public, s-maxage=60, stale-while-revalidate=300`; successful dossier responses carry `public, s-maxage=300, stale-while-revalidate=3600`; every error response carries `no-store`. The directives are shared-cache only — no browser `max-age` — so a CDN or reverse proxy absorbs repeated load while a buyer changing a filter still gets a fresh response rather than one their own browser is holding. The listing window is short because it is filtered, paginated, and should surface a new property quickly; a dossier is a stable per-slug document and tolerates longer. Errors are never cached so that a `404` cannot outlive the publish that resolves it. Why: this is portable across self-hosted and managed deployment, independent of Next's internal cache, and leaves the SEO decision where it actually belongs — page-level ISR on the dossier _page_, decided in step 6. Alternatives: `no-store` everywhere and defer — rejected, it makes every buyer request a database hit for no gain; prerendering the dossier JSON with `generateStaticParams` + `revalidate` — rejected, it couples the build to a reachable database and delays a re-publish behind a revalidation window, for a route no crawler reads.

---

**2026-09-02 — The `code` field of the API error envelope is a machine-readable slug, not the HTTP status restated; malformed input is rejected rather than repaired.**
Context: `api-spec.v1.md` defined the envelope as `{ "error": { "code", "message" } }` and then said "expected codes include `400`, `401`, ..." — leaving `code` genuinely ambiguous between a status restatement and a failure-class name. Step 3 had to implement one of them. Resolution: `code` names the failure class (`unknown_query_parameter`, `invalid_query_parameter`, `property_not_found`, `internal_error`); the HTTP status carries the status, and `message` is developer-facing prose naming the offending parameter. The spec has been amended so the ambiguity does not survive. Three validation rules were fixed at the same time and documented: a parameter given more than once is rejected unless it is `amenity`, the only repeatable one; an empty value (`?city=`) is rejected rather than silently treated as absent, since "every city" and "the empty-string city" are different requests and neither was intended; and integers are matched strictly, so `1.5`, `1e2`, and ` 1` are rejected rather than coerced into a fractional or surprising offset. Why: a `code` that restates the status carries no information the status line does not, and forces clients to parse prose to tell a bad `pageSize` from a bad `sort`. Alternatives: `code` as the status string — rejected for that reason; coercing malformed input to the nearest sensible value — rejected for the same reason `pageSize` is not clamped, since a silent repair lies to the caller about what it sent.

---

**2026-09-02 — The exclusion-list guard runs in production on every buyer response, not only in tests, and fails the request rather than filtering it.**
Context: `no-price.ts` existed from step 2 but was invoked only by tests, so a leak introduced by a later `select()` would be caught only if a test happened to cover that path. Resolution: `buyerJsonResponse` calls `assertNoExcludedData` on the real body immediately before serialisation, on both routes. A leak raises `BuyerResponseLeakError`, which the route turns into a `500` with a generic message; the offending field paths are logged server-side and never returned, since the paths themselves describe data a buyer response must not carry. Why: "no price ever reaches a buyer" is this product's central promise, and the last point at which it can still be kept is the response builder. Failing closed makes a leak a loud defect to fix at the source; quietly stripping the field would hide it while the next response reintroduced it. Alternatives: test-only enforcement — rejected, it protects only the paths a test already walks; silently stripping forbidden keys — rejected, it converts a defect into invisible data loss.

---

**2026-09-02 — Phase 2B uses one task branch for the whole phase (`task/phase-2b`), not one per step.**
Context: steps 0–2 each took their own branch, each cut from the previous rather than from `main`, so the history was already a single linear chain wearing four labels. The user asked for one branch per phase, merged to `main` at the phase boundary. Resolution: the step branches were renamed/collapsed into `task/phase-2b`; no commits moved, and `main` was untouched. `AGENTS.md`'s branch rule is amended to say the branch scope is the agreed phase. Why: the per-step branches conveyed nothing the commits did not, and four labels on one chain invite a merge order that does not exist. Alternatives: keeping per-step branches — rejected by the user.

---

**2026-09-02 — The Soft Gold verified badge takes a verified _fact_, never a boolean, and RERA registration alone does not earn it.**
Context: Phase 2B step 4 had to build `VerifiedBadge` such that Soft Gold "renders only when a concrete verified condition holds, never decoratively". A component taking `verified: boolean` satisfies that by convention only — any caller can pass `true`. Resolution: `VerifiedBadge` accepts a discriminated `VerifiedFact | null` and nothing else — no boolean, no `variant`, no `children`, no `className` — so there is no shape of the component that renders gold decoratively, and `null` renders nothing at all. The fact is derived by `reraVerifiedFact()`, which requires **both** `rera_registered` and a non-empty `rera_registration_number`: a registration flag with no registration number is a claim with no evidence behind it, and `design.v1.md` requires a trust signal to have an understandable evidence path. The badge displays the registration number it is asserting, so the claim and its evidence are inseparable. A property that is registered but whose number is unrecorded still shows its RERA facts through `FactValue` — it simply does not wear the badge. Why: this is the "evidence-led trust, no decorative trust signals" principle expressed as a type rather than a code-review habit. Note the current reachability, which is deliberate and not a defect: no code path sets `properties.rera_registered`, which defaults to `false`, and `docs/schema` records against the `property.rera_registration_number` field that "OCR never sets RERA verification or a verified badge". The badge is therefore unreachable until a real verification path exists (the GujRERA cross-check job, 2026-08-31) — which is the correct state for a trust signal that must never be produced by extraction alone. Alternatives: `<VerifiedBadge verified={property.reraRegistered} />` — rejected, it makes the guarantee a convention; treating `registered: true` as sufficient — rejected, it puts gold on an unevidenced claim.

---

**2026-09-02 — Honest incompleteness has a fixed vocabulary: "Not stated" and "Not offered", rendered distinctly, with the recorded status always beating a stray value.**
Context: the catalog records three states for a controlled fact, and `design.v1.md` requires `not_stated` and `explicitly_not_offered` to be shown rather than replaced with plausible placeholders — but no user-facing wording existed, so every screen would otherwise invent its own. Resolution: `FactValue` (`src/components/buyer/fact-value.tsx`) owns the vocabulary. `not_stated` renders "Not stated" (an unanswered question), `explicitly_not_offered` renders "Not offered" (an answered one), and the two differ in wording, styling, and explanatory `title` — never in colour alone, and never in the destructive colour, because an amenity a developer does not offer is a fact and not an error. Three supporting rules: an explicit status wins over a value passed beside it, so a value sitting next to a `not_stated` status surfaces as a data defect instead of being rendered as though correct; `available` with no text of its own renders the affirmation "Available" rather than a blank, which is the amenity case; and zero is a stated value, not absence, so a property with none of something keeps its real answer. Why: the difference between "nobody asked" and "the developer said no" is decision-relevant information, and a UI that renders both as a dash silently invents a fourth meaning the catalog does not hold. Alternatives: leaving wording to each screen — rejected as guaranteed drift; a single "—" for both absence states — rejected, it is exactly the plausible placeholder the design guide forbids.

---

**2026-09-07 — The browse page calls the read layer directly rather than fetching its own HTTP API, and re-uses that API's validator and leak guard to do it.**
Context: Phase 2B step 5 is the first page component in the codebase that needs published data, so nothing established how a screen reads. The obvious option was a server-side `fetch` of the step 3 route, which the phase plan leaned toward. Resolution: `src/app/properties/page.tsx` calls `listPublishedProperties(db, params)` directly, and deliberately keeps the two things the HTTP edge contributes — query parameters are validated by the same `parseListParams` the route uses, so the page cannot accept a query the API would reject, and the result passes through the same `assertNoExcludedData` guard `buyerJsonResponse` applies, so a price reaching the read layer fails the page as loudly as it fails the API. Why: a same-process server component fetching its own HTTP route needs an absolute origin URL it has no reliable way to know, adds a network hop to every render, and hands back JSON typed only by assertion — while the caching the route's `Cache-Control` expresses is for shared caches serving API consumers, not for a dynamic page render. Calling the query keeps types checked end to end. Alternatives: `fetch("/api/v1/properties")` from the server component — rejected for the reasons above; calling the query while skipping the validator and guard — rejected, that is how a page quietly becomes more permissive than the contract it claims to implement.

---

**2026-09-07 — The browse screen filters through a plain `GET` form, and the page canonicalises the resulting query string with a redirect.**
Context: the filter controls need to produce a query string, and the strictest rules of the step 1 contract collide with how HTML forms work: a `GET` form submits every control it owns, so an "Any city" option emits `?city=`, which the API correctly rejects with `422` because over the API an empty value is a broken request rather than an absent filter. The same applies to `sort=newest`, which a form always submits even when it changes nothing. Resolution: the filter form is a plain `GET` form with no client state and no `"use client"`, and the page canonicalises the incoming query first — dropping empty values and explicit defaults, keeping contract order — then redirects when canonicalisation changed anything. Canonicalisation drops only what a form could not help sending: a malformed value survives it untouched and is still reported, and an unknown parameter is preserved so it is still rejected as unknown. Why: this keeps every filtered view a real, shareable, bookmarkable address that works before any JavaScript arrives, without weakening the API's validation or forking a second, laxer parameter contract for the page. Alternatives: a Radix `Select` with client-side URL updates — rejected, it makes the whole browse screen a client component to solve a problem the platform already solves; stripping empty values inside the shared parser — rejected, it would loosen the API contract to suit a form; failing the page with a `422` on `?city=` — rejected, the buyer did nothing wrong.

---

**2026-09-07 — The property summary card shows no image, no verified badge, and no save/compare control — each an absence decided rather than overlooked.**
Context: step 5's card had to be built while two things it might plausibly show are unavailable. Resolution and reasons, in order. **No image:** `primaryMedia.gcsPath` is a Google Cloud Storage path, not a browser-fetchable URL, and media delivery (public bucket, signed URLs, or a proxy route) is an open decision gate that blocks step 6; rendering the path as a `src` would produce a broken image on every card that has media. The card holds a neutral, `aria-hidden`, textless frame in the layout so resolving that gate later is a change of one element rather than a re-layout. The frame deliberately makes no "no photo" claim, because a property may well have one this build cannot yet display — a caption saying otherwise would be a fabricated fact about the property. **No verified badge:** `VerifiedBadge` refuses to render without the RERA registration number that is its evidence (2026-09-02), and `PropertySummary` carries `reraRegistered` but not the number — only `PropertyDossier` has it. The card therefore _cannot_ construct a verified fact, and the badge stays where its evidence is. **No save or compare:** both are Phase 3 and depend on routes that do not exist; a disabled control promising them would be decoration. Why: each of these is the kind of gap that gets filled by a plausible-looking placeholder if it is not decided explicitly, and all three are covered by tests that fail if the absence is reversed. Alternatives: widening `PropertySummary` with `reraRegistrationNumber` so the card could show a badge — rejected, it is a step 1 contract change made as a side effect of a listing-grid task, and the badge is unreachable in real data either way; resolving the media gate inside step 5 — rejected, it is a decision about storage access and cost that belongs to step 6, made deliberately.

---

**2026-09-07 — Browse filter options are derived from published data, not from the catalog tables in full.**
Context: a filter control needs real options; asking a buyer to type a lookup key like `2bhk` would be the contract leaking into the UI. `listFilterOptions` (`src/lib/properties/filter-options.ts`) supplies them. Resolution: every vocabulary is derived from what is actually published — cities and localities distinct across `properties`, property types and BHK types distinct across published rows and variants, and amenities only where a published property records one as `available`, matching how the amenity filter itself matches. `possessionStatus` is the exception and needs no query: it is a fixed database enum, so its three values are known at compile time. This is not part of the buyer API contract and has no route. Why: publishing writes a `property_amenities` row for _every_ catalog amenity — the selected ones `available` and the rest `not_stated` — so a status-blind query would offer the entire 26-item catalog, every unselected entry of which returns an empty result set and reads as a broken screen. Offering only what can match keeps the control honest and prunes itself as the catalog grows. The rule is held by a database-backed test that fails if the status filter is dropped, verified by removing it and watching the count reach the full catalog. Alternatives: free-text inputs for lookup keys — rejected; offering the full catalog vocabularies — rejected for the reason above.

---

**2026-09-07 — Media delivery is deferred past Phase 2B; the dossier lists the media inventory without serving any file.**
Context: this gate blocked step 6 and had to be resolved before the dossier was built. Two findings changed the shape of the question. First, `property_media.media_type` includes `brochure_pdf`, so buyer-facing media can itself be a brochure — the exact document class that carries price lists — and `ARCHITECTURE.md` puts brochures, photos and floor plans in the same Google Cloud Storage. A public bucket is therefore not a small convenience decision; it needs a hard separation between source documents and buyer media, and an unanswered product question about whether a brochure should ever be buyer-facing. Second, nothing can populate media in this phase: the OCR field contract has no media field, so rows only begin to exist when developer upload lands in a later phase, and there is currently no GCS SDK installed and no credentials configured. Resolution: the gate moves to the phase that actually has media. The dossier renders with zero media correctly — which it had to do regardless — and, where rows do exist, lists the media _inventory_ (type and caption, both published facts) without fetching any file. Why: choosing between a public bucket, signed URLs, and a proxy route is expensive to reverse and there is nothing to validate the choice against; and the distinction between "this property has no media" and "this property has media you cannot see here yet" is a real one that a single empty state would collapse into a false statement about the property. Alternatives: a proxy route now — rejected as speculative work that would serve no byte in 2B, though it remains the strongest candidate when the gate is taken, since it keeps objects private and its URLs survive ISR; signed URLs — additionally rejected because expiring URLs fight the ISR-cached HTML this page now uses; a public bucket — rejected pending the brochure separation above.

---

**2026-09-07 — `PropScoreDial` does not ship in Phase 2B.**
Context: the second gate blocking step 6. Resolution: deferred entirely, as the phase plan itself recommended. Why: `prd.v1.md` lists the component's user-facing definition and calculation as an open product decision and requires that it not imply unsupported verification or a fabricated score — and no calculation is defined. A dial built from the current catalog would be a number the data does not support, which is exactly what the honest-incompleteness and evidence-led-trust rules forbid; it would also be the decorative trust signal `design.v1.md` rules out, in a more damaging form than a misplaced gold badge, because a score looks like a measurement. The component appears only in the Stitch export, which `design.v1.md` states is reference material and not a behavioural specification. Alternatives: shipping a composite score from available facts — rejected, it invents a weighting nobody has agreed and presents it as an assessment.

---

**2026-09-07 — The property dossier is incrementally statically regenerated, with paths rendered on first visit rather than prerendered at build.**
Context: SEO/ISR maturity was an explicit reason for choosing Next.js (2026-08-31), and step 3 deliberately left page-level caching to the page rather than the API. This is that page. Resolution: `src/app/properties/[slug]/page.tsx` exports `revalidate = 3600` and a `generateStaticParams` that returns an empty array; `dynamicParams` keeps its default of `true`. The mechanics were checked against the Next 16 docs bundled in `node_modules/next/dist/docs` rather than assumed — those docs state that returning an array, even an empty one, is what keeps the route statically rendered, and that an empty one means every path is rendered on first visit and cached thereafter. `next build` confirms the route as `● (SSG)`. Why: a dossier is a stable per-slug document and is the page organic search actually lands on, so prerendered HTML is the right shape; the one-hour window suits content that changes only when a reviewed submission is published. Returning no paths avoids requiring Postgres to be reachable during `next build`, which step 3 already flagged as a blocker, and `dynamicParams` means a property published after the last build is served on its first request instead of 404-ing until a redeploy. The API's own five-minute dossier `Cache-Control` is untouched; it serves clients calling the route directly. Alternatives: querying every slug in `generateStaticParams` — rejected, it needs a build-time database and lengthens builds as the catalog grows; a dynamic page with `Cache-Control` — rejected, it gives up the prerendered HTML that motivated the framework choice.

---

**2026-09-07 — The dossier's structured data models a residence, never an offer.**
Context: the step 6 checklist requires no price anywhere "including in metadata and structured data", and the property page is where JSON-LD would normally go. Resolution: `dossierJsonLd` emits schema.org `ApartmentComplex` — a `Residence` — and never `RealEstateListing`, `Offer`, or any price property. Amenities map honestly rather than optimistically: `available` becomes a `LocationFeatureSpecification` with `value: true`, `explicitly_not_offered` becomes `value: false` (a stated fact worth publishing), and `not_stated` is omitted entirely, because no claim has been made either way and structured data has no "unknown" a consumer would read correctly. No `url` is emitted, since the absolute origin is not knowable in the component and a guessed one is worse than none. The output is run through the same `findForbiddenKeys` guard that protects API responses. Why: an offer exists to state a price, so modelling the page as one would force a choice between fabricating a price and publishing a conspicuously priceless offer; a residence describes the building, which is what this catalog holds. Alternatives: `RealEstateListing` with the price omitted — rejected for that reason; no structured data at all — rejected, it discards the SEO value that motivated the ISR treatment above.

---

**2026-09-07 — `rera_registered: false` renders as "Not stated", never as "Not registered".**
Context: the dossier surfaces RERA facts, and the column is a non-null boolean defaulting to `false`. Resolution: a false flag renders through the standard absence vocabulary, exactly as a null would; only `true` renders "Registered". Why: no code path sets `properties.rera_registered` — the field contract records that OCR never sets RERA verification, and the flag waits on the GujRERA cross-check job (2026-08-31) — so `false` means "no registration has been recorded here", which is emphatically not the claim "this project is not RERA registered". Publishing the second statement about a real project would be a fabricated fact of the most damaging kind available on this surface, since it is an accusation of non-compliance. A boolean column whose false value is indistinguishable from "unasked" must be read as unasked. Alternatives: rendering "Not registered" — rejected as above; hiding the RERA section when the flag is false — rejected, it conceals that the question is open.

---

**2026-09-07 — Unrecorded catalog facts stay on the dossier but sit behind a counted disclosure.**
Context: the amenity catalog holds 26 entries and publishing writes a row for every one of them, so a typical property has a handful stated and the rest `not_stated`. Rendering them flat produced a wall of "Not stated" that buried the few real answers — found by looking at the rendered page against real published data, not by reasoning about it. That is the table dump `design.v1.md` rules out, and the step 6 checklist explicitly asks for facts organised progressively instead. Resolution: stated facts (`available` and `explicitly_not_offered`) lead the section grouped by category; the unrecorded ones sit inside a `<details>` whose summary states their count ("24 not recorded for this property"). Every row remains in the markup, grouped and labelled, and `<details>` opens without JavaScript. Why: this is progressive disclosure, not concealment — the count is stated up front, nothing is filtered out of the page, and the reader can still see exactly which questions are unanswered. The alternative reading, that any collapse violates honest incompleteness, is answered by the summary naming the number: a buyer is told how much is unknown before deciding whether to expand. Alternatives: listing every row flat — rejected as the table dump above; omitting `not_stated` rows entirely — rejected outright, it destroys the distinction between an unasked question and a refused one, which is the whole point of the three-state vocabulary.

---

**2026-09-07 — The landing page reads no data and prerenders as static HTML.** _(Partly superseded on 2026-09-07 by "The landing page carries a recently-published strip and becomes `force-dynamic`", below. The reasoning about inventing a "featured" ordering still stands and is carried forward; the static rendering does not.)_
Context: step 7 had to decide whether the landing page shows catalog content — a strip of recently published properties was the obvious candidate, and it would have reused `PropertyCard` and the existing `newest` ordering honestly. Resolution: the landing is static. It states the proposition, explains how the catalog treats facts, explains the absence of prices, and routes to browse and guided intake; it issues no query, so `/` prerenders at build with no database dependency (`next build` reports it as `○ (Static)`). Why: nothing on the page varies by request, visitor, or catalog state, and it is the most-visited and most-cacheable page in the product; keeping a database call off it is consistent with the reasoning that kept build-time Postgres out of the dossier's ISR. It also avoids inventing a "featured" ordering the catalog cannot justify, and avoids designing an empty strip against an empty table — there is no real content to look at yet, and the honest time to design a content strip is when there is. Alternatives: a recent-properties strip now — deferred to step 9's convergence work, where real published properties exist and the strip can be judged against real content; a "featured properties" selection — rejected outright, since no ranking exists that the data supports.

---

**2026-09-07 — Landing calls to action take their destinations from `BUYER_NAV`, not from their own copy.**
Context: the header has linked `/properties` and `/intake` since step 4, and the landing page needed the same two entry points. Resolution: `LandingScreen` destructures `BUYER_NAV` and uses its `href` and `label` values rather than restating the paths, and a test asserts the rendered links contain exactly those hrefs. Why: two hand-written copies of a route are the smallest possible version of the divergence problem this project exists to avoid — a route that moves would leave one of them pointing at the old path, silently. Note that `/intake` returns `404` until step 8 lands; this is expected, the header has had the same dead link since step 4, and nothing is merged to `main` before the phase boundary. Alternatives: writing the paths inline — rejected as above; omitting the intake call to action until step 8 — rejected, the step 7 checklist requires entry points into both browse and guided intake, and the gap closes on the same branch before merge.

---

**2026-09-07 — "Persona priorities" gets a fixed six-key vocabulary, defined in code, with every key grounded in a fact class the catalog publishes.**
Context: step 8 required capturing "persona priorities" and no definition of one existed anywhere in the repo. `buyer_intake_sessions.persona_priorities` is unshaped `jsonb`, `prd.v1.md` says only "capture buyer priorities", `buyer.md` says only "choose persona priorities", and the sole concrete hint was a non-normative `-- e.g. ["family_space","privacy","location"]` comment in `schema.v1.md`. Resolution (user sign-off): six keys in `src/lib/properties/intake.ts` — `family_space`, `location`, `possession_speed`, `amenities`, `privacy`, `build_quality` — each carrying a `grounding` string naming the published facts it reads (unit areas and room dimensions; city and locality; possession status and date; recorded amenities; tower and unit counts; published specifications), shown to the buyer alongside the option. A buyer may choose at most three. Why: an ungrounded priority is a question asked in bad faith — it promises the catalog can answer something it cannot — and a vocabulary invented silently would have become a second de-facto contract the moment Phase 3 built matching against it, which is the failure mode this project exists to prevent. It lives as a code constant rather than a catalog + synonym table because `persona_priorities` is `jsonb` by design, nothing persists it this phase, and `AGENTS.md`'s catalog rule names amenities and specifications specifically. Alternatives: the schema comment's three keys alone — rejected as too thin a question and as omitting possession timing, one of the strongest signals the catalog publishes; free text — rejected, it defers the vocabulary at the cost of an uncontrolled field and makes the answer usable for nothing.

---

**2026-09-07 — Intake answers live in client state and never enter the URL, breaking deliberately from the browse screen's URL-as-state pattern.**
Context: the browse screen (step 5) puts its entire filter set in the query string so a filtered view is a real, shareable, back-button-reachable address, and that pattern was the obvious one to copy. Resolution: `IntakeFlow` is a `"use client"` component holding every answer in `useState`; no answer is written to the URL, to storage, or to a server. It is the only client component in the buyer surface, and the boundary is drawn as tightly as possible — `IntakeScreen`, the page frame, and the standing copy all stay on the server. Why: intake captures a range the buyer states about their own finances, and a query string lands in browser history, in server access logs, and in the `Referer` header of every subsequent request. Putting it there is the closest this application could come to publishing a monetary figure, which is the one thing the whole product is built not to do. The shareability that justifies URL state on browse is worth nothing here and actively harmful. Alternatives: URL state for consistency — rejected as above; `sessionStorage` — rejected, it survives the tab and buys nothing, since nothing needs to outlive the flow.

---

**2026-09-07 — Guided intake ends by handing off to `/properties` carrying only city and configuration.**
Context: matching is Phase 3 and `POST /api/v1/intake-sessions` is an explicit non-goal of Phase 2B, so intake cannot produce matches and the flow had no natural ending. Resolution (user sign-off): the final step restates every answer as a brief, then links to `/properties` pre-filled with the two answers that map to filters the read contract actually has — `city` and `bhk`. The priorities and the stated range are used for nothing. `handoffParams` names those two fields one at a time rather than spreading the answers object, so a future field cannot reach the URL by an inattentive edit, and the summary states in words which filters the link carries and that the range is not among them. Why: it turns intake into something real rather than a dead end, without inventing a match the catalog cannot justify — and saying plainly what the link does is what stops the result set reading as "properties chosen for me". Alternatives: a summary that dead-ends with an unfiltered browse link — rejected, it discards two answers that map to genuine filters and makes the buyer re-enter them; mapping priorities onto amenity filters as well — rejected, that mapping is defined by no document and would be exactly the second de-facto contract this project guards against.

---

**2026-09-07 — The stated range is captured with a coarse two-handle slider, built from native range inputs.**
Context: the step 8 checklist requires the range be presented as a stated preference, "never as a price or a bucket". Three controls were put to the maintainer: two typed figures, a dual-handle slider, and a list of preset bands. Resolution (user's explicit choice): the slider, with mitigations for the concern raised against it — it steps in five-lakh notches rather than rupees so no exact figure is implied, its top end is open-ended ("₹5 crore or more") so it never puts a ceiling in the buyer's mouth, it reads back as "You said …", and no property figure is ever shown against it. Preset bands were rejected on the record: a preset list of ranges is a bucket in all but name, and buckets are the private matching mechanism (`ARCHITECTURE.md`), not a buyer-facing control. Implementation note: two overlaid native `<input type="range">` elements rather than Radix's `Slider`, because each handle then has a real labelled form control that assistive technology and tests can address, and because Radix's slider measures itself with `ResizeObserver`, which jsdom does not implement — the one interactive control in this phase would otherwise have been the one its tests could not drive.

---

**2026-09-07 — Client intake state is named `statedRange` / `fromLakh` / `toLakh`, never after the `buyer_intake_sessions` columns.**
Context: the columns are `budget_min_inr` and `budget_max_inr`, and mirroring them in client state was the obvious naming. Resolution: the client shape is `statedRange: { fromLakh, toLakh }`, and a test asserts the whole answers object passes `findForbiddenKeys`. Why: `no-price.ts` matches `/inr/i` and runs in production, not only in tests — an `Inr`-suffixed key would trip the guard, leaving a choice between weakening the product's central structural guard and carrying a shape that can never be handed to anything. The naming avoids the trade entirely and says what the value actually is: a range the buyer stated, not a price.

---

**2026-09-07 — `/intake` is `force-dynamic`.**
Context: the route reads `listFilterOptions` so its questions offer only configurations and cities something published can match. `next build` reported it as `○ (Static)`: `listFilterOptions` is a Drizzle query rather than a `fetch`, so Next.js cannot tell it is uncached, and `dynamic: "auto"` rendered the page once at build. Resolution: `export const dynamic = "force-dynamic"`; the route table now reports `ƒ /intake`. Why: a prerendered page would freeze the questions to whatever was published on build day, and `/properties` is dynamic against the same query — two screens offering different vocabularies for the same catalog would be the divergence problem in miniature. Alternatives: an ISR window as on the dossier — rejected, this page is far less trafficked than the dossier and the staleness buys nothing; leaving it static — rejected, it is a correctness bug, not a caching preference.

---

**2026-09-07 — The landing page carries a recently-published strip and becomes `force-dynamic`. Supersedes "The landing page reads no data and prerenders as static HTML" (2026-09-07) in part.**
Context: step 7 deferred this strip to step 9 explicitly, on the grounds that a content strip should be designed against real content rather than an empty table. Step 9 published real properties, so the condition it was waiting on is met, and the maintainer chose to build it. Resolution: `LandingScreen` takes a `recent: PropertySummary[]` prop and renders the six most recently published properties through the existing `PropertyCard`, in the catalog's own `newest` order; the section is omitted entirely when the list is empty rather than rendering an empty shelf. `/` reads the list in `src/app/page.tsx` and exports `dynamic = "force-dynamic"`. **Not ISR:** a route with no dynamic segment has no `generateStaticParams` escape hatch, so `export const revalidate` prerenders at build and requires Postgres reachable during `next build` — the constraint step 3 flagged and step 6 worked around on the dossier. That was measured, not assumed: building against an unreachable `DATABASE_URL` succeeds as written and fails with `revalidate = 3600` (`Error occurred prerendering page "/"`, `ECONNREFUSED` on the listing count query). Why: the strip is what makes the landing page concrete rather than purely declarative, and "recently published" is a fact the data carries — unlike "featured" or "best", which would be an assessment nothing in this catalog supports. The cost is the one this entry supersedes: `/` is no longer the product's cheapest page. It is one indexed `order by ... limit 6`, and repeated landing traffic belongs in a reverse proxy's shared cache, which is already the assumption behind the API routes' own `Cache-Control`. Alternatives: ISR with a revalidate window — rejected, it couples `next build` to a live database, which this project has twice decided against; `use cache` for a cached read with no build-time dependency — rejected for step 9, since it requires enabling `cacheComponents` project-wide and that changes caching semantics on every route; fetching the strip client-side from the public API to keep `/` static — rejected, it hides the catalog from crawlers on the one page most likely to be indexed, which is the opposite of why ISR was chosen for the dossier.

---

**2026-09-07 — The Phase 2B convergence properties are published through the real transaction and left in the local database.**
Context: step 9's acceptance is that real properties, approved through the actual publish transaction, render correctly on buyer pages built against fixtures. Steps 5–7 created test properties and removed them afterwards to leave the maintainer's database as found. Resolution (user sign-off): five properties are published through `publishSubmission` — never a direct catalog insert, per the one-write-path rule, which binds seed scripts as firmly as it binds application code — and left in place. They were created by a throwaway script that constructs an approved `property_submissions` row plus confirmed `property_submission_fields`, and the script was deleted after running; nothing about them is committed, so `main` gains zero rows of data. The set is chosen to exercise the surface rather than to look full: three possession statuses, three property types, a property with all three area bases and one with only carpet, a bungalow with built-up and super built-up but deliberately no carpet, a plot with no unit variants at all, and one deliberately sparse record with no specifications and a single amenity. Why: steps 5–7 were testing and cleaned up correctly; step 9 is the convergence deliverable, and a local catalog with real content means the product can actually be run and looked at — including guided intake, whose questions offer nothing at all against an empty catalog. Alternatives: publish, verify, then delete — rejected as leaving the acceptance criterion provable only from a transcript; a larger seed — rejected, five records already cover every distinct rendering path and more would be clutter.

---

**2026-09-07 — Phase 2B ships with no `explicitly_not_offered` amenity anywhere in the catalog, and this is a contract gap rather than a rendering bug.**
Context: the buyer surface renders "Not stated" and "Not offered" as distinct published facts (2026-09-02), and step 9's real data produced 0 rows in the second state. Resolution: recorded, not worked around. The publish transaction marks every amenity a submission does not list as `not_stated`, and the active `property_schema_fields` contract has only `property.amenities` — an array of keys that are available — so no submission origin can currently assert that a developer answered "we do not offer this". The distinction remains implemented, tested, and correct on the read and render side; nothing in the catalog can reach the second state yet. Why: the honest-incompleteness rule is about not collapsing two different facts into one, and it is upheld — the gap is that one of the two facts has no input path, which is a field-contract question for the phase that owns developer submission, not something to fake with a seed script. Alternatives: writing `explicitly_not_offered` rows directly so the state is visible — rejected outright, it would bypass the publish transaction and fabricate a claim about a real developer; adding a negative-amenity field to the contract now — rejected as out of scope for 2B and belonging with the developer portal.
