# Local self-hosted Postgres (see docker-compose.yml)
DATABASE_URL=postgres://propcompare:propcompare_dev_only@localhost:5432/propcompare

# Better Auth
BETTER_AUTH_SECRET=replace-with-a-real-32-byte-secret
BETTER_AUTH_URL=http://localhost:3000

# Google Cloud Storage (media)
GCS_BUCKET=
GCS_PROJECT_ID=
GCS_CLIENT_EMAIL=
GCS_PRIVATE_KEY=
